<div class="d-flex flex-column justify-content-center align-items-center vh-100">
  <div class="card mb-3 px-4 pt-4 pb-2" style="width: 24rem;">
    <div class="card-body">
      <div class="instagram-logo-link"></div>
      <p class="text-center text-secondary fs-6 fw-semobold">註冊即可查看朋友的相片和影片。</p>
      <div class="d-flex justify-content-center">
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "w-100" }) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>

          <div class="mb-3">

            <%= f.email_field :email, placeholder:"電子郵件地址",autofocus: true, autocomplete: "email", class: "form-control" %>
          </div>

          <div class="mb-3">
            <%= f.password_field :password, placeholder:"密碼", autocomplete: "new-password", class: "form-control" %>
          </div>

          <div class="mb-3">
            <%= f.password_field :password_confirmation, placeholder:"確認密碼",autocomplete: "new-password", class: "form-control" %>
          </div>

          <div class="actions">
            <%= f.submit "註冊", class: "btn btn-primary insta-primary w-100"%>
          </div>
        <% end %>
      </div>
    </div>
  </div>
  <div class="card" style="width: 24rem;">
    <div class="card-body py-4">
      <p class="card-text text-center text-body-secondary mb-0 fs-6">已有帳號嗎? <%= link_to "登入", root_path, class:"standard_link"%> </p>

    </div>
  </div>
</div>
